class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
       result = []
       for n in nums:
        result.append(n)
       for n in nums:
        result.append(n)
       return result
